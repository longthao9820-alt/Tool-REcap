[project]
name = "cong-cu-video-recap"
version = "0.1.0"
description = "Ứng dụng desktop tiếng Việt dựng video recap từ JSON."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
